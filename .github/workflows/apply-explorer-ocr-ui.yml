# Retired. Explorer OCR wiring is on main. Kept as dispatch-only so old badge links do not 404.
name: apply-explorer-ocr-ui (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired no-op — explorer OCR already on main."
