# Obsolete one-shot patcher. Explorer OCR Open/Result wiring is already on main.
# Previous file had invalid YAML (nested triple-quotes broke the workflow parser),
# which caused a red "Invalid workflow file" check on every push.
name: apply-explorer-ocr-ui

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Patch already applied on main
        run: |
          echo "ExplorerScreen FileDetails onOpen/onOcr + OcrResultDialog are already on main."
          echo "This workflow is kept only so old links do not 404; it performs no changes."
