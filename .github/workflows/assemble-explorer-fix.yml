# Retired one-shot assembler for a deleted fix branch. Dispatch-only to avoid push noise.
name: assemble-explorer-fix (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired — sources already restored on main."
