# Copy to .env (gitignored) or inject via CI secrets / secrets Gradle plugin.
# Never commit real keys.

# Optional Gemini / Firebase AI
# GEMINI_API_KEY=

# Google OAuth client ID (used by GoogleDriveAuth / requestIdToken)
# From Google Cloud project thevvforg — installed/desktop client; also add Android client with package com.vvf.smartmanager if sign-in fails.
GOOGLE_WEB_CLIENT_ID="1058219630435-tav6g387ubaooipm0k89g68c2grhcne6.apps.googleusercontent.com"
