# Copy to .env (gitignored) or inject via CI secrets / secrets Gradle plugin.
# Never commit real secrets (API keys). OAuth client IDs are public identifiers.

# Optional Gemini / Firebase AI
# GEMINI_API_KEY=

# Google OAuth client ID (GoogleDriveAuth / requestIdToken)
# Project thevvforg — also create Android OAuth client with package com.vvf.smartmanager + SHA-1 if needed
GOOGLE_WEB_CLIENT_ID="1058219630435-tav6g387ubaooipm0k89g68c2grhcne6.apps.googleusercontent.com"
